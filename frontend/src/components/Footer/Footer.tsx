import styled from 'styled-components';

const SWrapper = styled.div`
  width: 100%;
  padding: 18px 10px;
  border-top: 1px solid #777;
`;

export default function Footer() {
  return <SWrapper>My Footer @2021</SWrapper>;
}
